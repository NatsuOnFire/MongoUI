<h2>Add Database</h2>
<section>
    <?php if ($this->error): ?>
        <p class="error">Des erreurs sont intervenues durant la validation du formulaire.</p>
    <?php endif; ?>
    <p>
        <?php
        $form = $this->mongoAddDatabaseForm;
        $form->setAttribute('action', $this->url(null, ['controller'=>'Database', 'action'=>'add']));
        echo $this->form()->render($form);
        ?>
    </p>
    <a href="/mongomyadmin/admin"><< return Home</a>
</section>